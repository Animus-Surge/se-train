[project]
name = "setrain-server"
version = "0.1.0"
description = ""
authors = [
    {name = "Surge",email = "esfloyd9341@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14,<4.0"
dependencies = [
    "fastapi (>=0.136.0,<0.137.0)",
    "loguru (>=0.7.3,<0.8.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
